[build]
  command = "ls -la && ./gradlew clean assembleDebug --no-daemon"
  publish = "app/build/outputs/apk/debug/"

[build.environment]
  ANDROID_HOME = "/opt/android"
  JAVA_HOME = "/opt/java"
  GRADLE_OPTS = "-Xmx2048m -Dorg.gradle.daemon=false"